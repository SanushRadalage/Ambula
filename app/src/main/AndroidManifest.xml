<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.intpro.decepticons">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".AutoStart">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <service
            android:name=".TheService"
            android:enabled="true">
        </service>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:screenOrientation="portrait"
            android:name=".EmergencyMap"
            android:label="@string/title_activity_maps" />
        <activity android:name=".DriverLog"  android:screenOrientation="portrait"/>
        <activity android:name=".PatientLog" android:screenOrientation="portrait" />
        <activity android:name=".PatientRegister" />
        <activity android:name=".DriverRegister" />
        <activity
            android:name=".DriverMap"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_driver_map" />
        <activity
            android:name=".PatientMap"
            android:label="@string/title_activity_patient_map" />
        <activity android:name=".DriverProfile" />
        <activity
            android:name=".BlindMap"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_blind_map" />
        <activity android:name=".PatientProfile"></activity>
    </application>

</manifest>